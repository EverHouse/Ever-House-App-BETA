Below is the list of critical fixes and a new stability issues we want fixed. verify that these changes were made:
1. Fix Booking Resilience: In src/pages/Member/BookGolf.tsx, inside the fetchAvailability function, change await Promise.all(...) to await Promise.allSettled(...). Ensure that if one resource fails to load slots, the others still display.
2. Fix Admin Resilience: In src/pages/Admin/AdminDashboard.tsx (SimulatorAdmin component), replace Promise.all with Promise.allSettled for the initial data fetch so the dashboard loads even if one API endpoint fails.
3. Fix Mobile Scroll Conflict: In src/components/PullToRefresh.tsx, add the class overscroll-y-contain (or inline style overscrollBehaviorY: 'contain') to the main container div. This prevents the native browser refresh on mobile.
4. Fix Dashboard Height: In src/pages/Member/Dashboard.tsx, change the PullToRefresh className from h-screen to h-full to prevent the bottom navigation from being pushed off-screen on mobile.
5. Optimize Landing Page: In src/pages/Public/Landing.tsx:
    * Replace the hero div background image with a standard <img src="/images/hero-lounge-optimized.webp" class="absolute inset-0 object-cover..." /> for faster LCP.
    * Replace navigation <button> elements with <Link> components for better SEO."
Code Reference for (Booking Fix)
Here is how the BookGolf fix should look:
TypeScript



// src/pages/Member/BookGolf.tsx

// Replace the existing Promise.all block with this:
const results = await Promise.allSettled(resources.map(async (resource) => {
  const { ok, data: slots } = await apiRequest<APISlot[]>(
    `/api/availability?resource_id=${resource.dbId}&date=${selectedDateObj.date}&duration=${duration}`
  );
  return { resource, ok, slots };
}));

results.forEach((result) => {
  if (result.status === 'fulfilled' && result.value.ok && result.value.slots) {
    const { resource, slots } = result.value;
    // ... existing logic to process slots ...
    slots.forEach(slot => {
        if (!slot.available) return;
        // ... (rest of your existing logic to add to allSlots)
    });
  }
});
